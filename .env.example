# Local Docker Compose only — NOT needed when running with Python directly (uses ~/.aws/credentials)
# NOT needed in App Runner (uses IAM role)
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key
AWS_DEFAULT_REGION=us-east-1

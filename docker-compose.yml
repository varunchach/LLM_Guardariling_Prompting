version: "3.9"

services:
  chatbot:
    build: ./app
    ports:
      - "8501:8501"
    env_file:
      - .env           # copy .env.example → .env and fill in AWS credentials
    volumes:
      - ./app/prompts:/app/prompts:ro
    restart: unless-stopped
